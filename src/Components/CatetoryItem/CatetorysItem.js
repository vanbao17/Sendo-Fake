import classNames from 'classnames/bind'
import styles from "./CatetoryItem.module.scss"
const cx = classNames.bind(styles)
function CatetorysItem() {
    return (
        <div className={cx('wrapper')}>
            
        </div>
    );
}

export default CatetorysItem;